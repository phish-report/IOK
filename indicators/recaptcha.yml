title: reCAPTCHA

detection:
  buttonElement:
    html|contains|all:
      - "g-recaptcha"
      - "data-sitekey"

  programaticInvoke:
    html|contains: "grecaptcha.execute('"

  condition: buttonElement or programaticInvoke

tags:
  - anti-analysis
