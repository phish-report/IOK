title: reCAPTCHA

detection:
  buttonElement:
    dom|contains|all:
      - "g-recaptcha"
      - "data-sitekey"

  programaticInvoke:
    dom|contains: "grecaptcha.execute('"

  condition: buttonElement or programaticInvoke

tags:
  - anti-analysis